무결성 : db 저장값과 현실의 값이 일치하는 정확성
개체무결성 : 기본키
참조무결성 : 외래키
도메인무결성 : 속성에 대해 정의하는 무결성 check, not null, unique

정규화

제 1 정규화(정규형) : 모든 속성은 반드시 하나의 값을 가져야 한다(반복 형태가 있어서는 안됨)  
	/어떤 하나의 값에 대하여 일치하여 반복되는 요소와 서로 다른 요소가 뒤섞여 있을 때에는 그것들을 분리하여 따로 빈을 만든당.

제 2 정규화(정규형) : 모든 속성은 반드시 기본키 전부에 종속되어야 한다(기본키 일부에만 종속되어서는 안됨)
	/예를 들어서 상품코드가 달라짐에 따라서 상품가격은 자동으로 달라지는 종속형태? 
	수량은 그럼 상품코드는 주문번호와 상품코드가 둘다 존재해야나타남.

제 3 정규화(정규형) : 기본키가 아닌 모든 속성 간에는 서로 종속될 수 없다(속성 간 종속성 배제)

이후 BDNF, 4, 5 는 생략





